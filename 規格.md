# AI 定位指令工具包 — 可驗收規格

> 這份文件的用途：任務 9 上線檢查時，拿這份對照產品，逐項確認「做完了沒」。
> 內容來自實際程式碼，由作者逐項確認過。

- 產品：AI 定位指令工具包（2026-09-15 由「AI 指令小幫手」更名）
- 定位：給正在經營 IG（社群平台）的人。填一次人設，之後輪播貼文、短影音、月主題的指令都自動帶入。
- 使用對象：客戶／不特定的人，各自拿去用（工具型，非系統型）

---

## 一、資料保存方式（任務 6 的結論）

**本機儲存（localStorage，瀏覽器本機儲存空間）＋ 匯出／匯入備份。不使用資料庫。**

診斷過程：
1. 只有自己用嗎 → 不只自己
2. 是固定認識的人還是客戶 → 客戶
3. 給客戶的是「各自拿去用的工具」還是「資料跑在作者這邊的系統」 → 各自拿去用

因此：
- 不需要資料庫、不需要帳號登入
- 不需要付雲端服務費用
- 作者看不到、也不需要看到客戶填了什麼
- 客戶換裝置：用匯出備份帶過去，**不是自動同步**

### 必須出現在畫面上的誠實文案
- 人設打底機頁尾與「備份」頁（原儀表板）最上方的資料儲存提醒卡：
  「只存在這台裝置的這個瀏覽器裡，不會上傳到任何地方。換裝置、換瀏覽器、清除瀏覽器資料都會不見。」
- 首頁新手引導的「🔒 資料儲存提醒」：本機儲存、建議固定同一台裝置與瀏覽器、定期匯出備份；
  「加入手機主畫面」只能寫成方便開啟，**不可暗示等於備份**
- 不可寫「雲端」「同步」「永久保存」
- 「匯入」是把備份**複製過去一次**，兩邊之後各自獨立，不會互相更新

---

## 二、頁面清單

| # | 頁面 | 說明 |
|---|---|---|
| 1 | 人設定位（人設打底機） | 13 關引導式填寫。**預設落地頁**。還沒按開始時，上方有首頁新手引導（主標題、3 步驟、為什麼先做人設、資料儲存提醒）；人設完成度 ≥ 60%（與指令產生器提醒同一條線）顯示「你的人設已建立 ✓」，不強迫重做 |
| 2 | 開始製作（原指令產生器） | 依類型產生指令。頁面標題「今天想做什麼？」；必填填完顯示「✦ 你的專屬 AI 指令已整理完成」（人設未達 60% 時改用誠實版文案） |
| 3 | 指令庫 | 收藏、搜尋、篩選、編輯 |
| 4 | 備份（原儀表板） | 最上方匯出／匯入備份與資料儲存提醒，下面保留使用概況、各類型、最近紀錄、最近刪除 |

選單名稱（手機底部與電腦側邊一致）：我的定位｜開始製作｜指令庫｜備份。頁面 id 仍為 persona／gen／list／dash，程式不變。

---

## 三、人設資料欄位

| 組別 | 欄位 |
|---|---|
| 角色 | AI 要扮演誰 |
| 基本 | 暱稱、IG 帳號、定位、專長、**身份／領域（可複選，含「其他」自行輸入）** |
| 特質語氣 | 溝通風格、內容能量、互動方式、自訂特質、語氣節奏、禁用語氣、備註 |
| 受眾 | 年齡層、受眾類型、其他受眾、痛點、第一句話、想要的改變 |
| 故事優缺 | 轉變前、轉折點、成果、優勢、弱項 |
| 變現問答 | 服務項目（可多筆）、常見問答（3 組起，可增減） |
| 範例 | 好句子（可多筆）、流量最好／最差的開頭、踩過的雷 |
| 行動收尾 | 希望讀者做什麼、預約方式、品牌關鍵詞、廣告用語已知悉 |

---

## 四、功能清單

### 人設
- 新增、切換、重新命名、複製一份、刪除、清空重填
- 多組人設並存，各自獨立
- 舊版單頁表單填過的資料，開檔時自動搬進新欄位（不讓資料掉）

### 指令
- 依類型產生、複製、儲存進指令庫、加入最愛、檢視全文、編輯、刪除
- 指令庫可搜尋、依類型／最愛篩選、依時間／名稱排序

### 備份
- 匯出備份（JSON 檔，檔名含日期）
- 匯入備份，分**合併**與**覆蓋**兩種模式
- 覆蓋前有二次確認
- 匯入檔案會先照欄位格式重洗一次，避免壞掉的檔案把畫面弄爛

### 身份／領域與舊資料相容
- 欄位 `identities`（陣列）＋ `identityOther`（「其他」的自行輸入）；舊版單選的 `industry` 字串**保留不刪**
- `identityV` 標記是否已轉換；轉過就不再轉（使用者清空複選後重整不會被舊值塞回來）
- 舊值轉換：「美業／醫美相關」→「美容／美睫／美甲／美髮」（不硬塞醫美，第 1 關提示可自行加勾「醫美相關」）；
  與新選項同名的一對一轉；不認得的值 →「其他」＋原文字放進自行輸入
- 開檔、匯入舊備份都會自動轉；匯出備份會帶上新欄位與標記

### 廣告用語提醒
- 身份／領域勾了「美容／美睫／美甲／美髮」或「醫美相關」任一個時觸發
- 第 13 關顯示衛生局禁用詞清單（暗示醫療業務／改變人體結構／宣稱醫療效能）與可改用說法
- 同一份規範會寫進產出指令的【背景】區塊

---

## 五、刪除行為

| 動作 | 刪除前二次確認 | 刪除後可還原 |
|---|---|---|
| 刪除單筆指令 | ✅ | ✅ 30 天 |
| 刪除這組人設 | ✅ | ✅ 30 天 |
| 清空這組人設 | ✅ | ✅ 30 天（清空前留快照） |
| 在「最近刪除」裡永久刪除 | ✅ | ❌ 真的救不回來 |

### 最近刪除的規則（必須照實寫在畫面上）
- 保留 **30 天**
- 逾期項目是在**使用者下次打開網站時**才被清掉，不是背景自動倒數（本機產品沒有伺服器）
- 上限 **200 筆**，超過從最舊的開始清
- **匯出備份不包含**最近刪除的項目，要保留需先還原再匯出

---

## 六、明確不做的事

- 不做會員登入、帳號密碼
- 不做雲端同步、跨裝置自動同步（曾做過，已完整移除，改用匯出／匯入）
- 不做多人共用、後台管理
- 不蒐集、不儲存客戶的任何資料到作者端

---

## 七、上線前必須通過（任務 9 用）

- [ ] 有真正的公開網址，不是電腦檔案路徑
- [ ] 換裝置、換瀏覽器、無痕視窗的行為與畫面說明一致
- [ ] 手機版可正常操作，已截圖
- [ ] 刪除有二次確認，且還原流程實機測過
- [ ] 沒填資料就送出會有提示，不是空白沒反應
- [ ] 按鈕有成功／失敗回饋
- [ ] 輸入亂碼、超長文字、表情符號不會炸版
- [ ] 網頁原始碼裡沒有管理鑰匙（本產品完全無對外連線，無金鑰）
- [ ] 有匯出／匯入備份，AI 改壞時能回到正常版本
- [ ] 至少三位真人盲測完成，全程使用假資料
