# 公開導覽頁第一版驗收

日期：2026-09-17。基礎版本：d00af89。

檢查發現 repository 最新版已包含完整 14 區導覽及 `?from=guide` 入口。本次直接完善既有頁，沒有另建工具或替換 App。

## 檔案與入口

- 修改 `guide/index.html`：第一屏補充產出指令說明、章節導覽、跳至正文連結、FAQ 錨點，移除導覽頁外部字型依賴。
- 新增 `guide/guide.css`：手機排版、深陶土色按鈕、章節捷徑、響應式與 focus 支援。
- 更新 `README.md`，新增本報告。
- `index.html` 原工具完全未修改。既有定位、多人設、指令庫、備份、localStorage keys、保存與 migration 邏輯完全沿用，不需新增 migration。
- 公開分享入口仍為 `/roxyai/guide/`；工具舊網址 `/roxyai/` 維持原行為，保留使用者書籤相容性。
- CTA 沿用 `../?from=guide`。既有程式讓空白新使用者進基礎定位 STEP 1；有資料者保留定位首頁，不強迫重填。參數處理後移除。
- 本機預覽：`http://127.0.0.1:4173/roxyai/guide/`。

## 20 項測試

| # | 檢查 | 結果 |
|---|---|---|
| 1 | 手機新手能否理解產品 | 內容／畫面檢查通過；真人理解程度待測 |
| 2 | 首屏主標 | 通過，375／390／430 可見完整主標 |
| 3 | 前半段不是另一個 AI | 通過，第 2 區明示 |
| 4 | 產出指令而非成品 | 通過，首屏、第 2／5／10 區均說明 |
| 5 | 第一次使用 5 步驟 | 通過，5 個有序步驟 |
| 6 | 同裝置＋同瀏覽器 | 通過，步驟、保存區及 FAQ 說明 |
| 7 | 加主畫面不等於備份 | 通過，保存區與 FAQ 明示 |
| 8 | FAQ 開合 | 通過，7 題 Enter／Space 開合，點按目標大於 44px |
| 9 | CTA 到定位 | 通過，新使用者進 STEP 1，有資料者保留原定位 |
| 10 | AI 定位 | 通過，編輯保存、重整、新增／複製／改名人設 |
| 11 | 開始製作 | 通過，所有現有類型可產生非空指令 |
| 12 | 指令庫 | 通過，儲存測試指令並重整仍存在 |
| 13 | 匯出 | 通過，實際下載 JSON 並核對人設內容 |
| 14 | 匯入 | 通過，獨立空白瀏覽器 context 上傳備份、點合併、核對及重整 |
| 15 | localStorage 重整 | 通過，人設、指令與導覽測試標記保留 |
| 16 | 全新使用者 | 通過，獨立 context 測試 |
| 17 | 舊版使用者 | 通過，模擬舊 aips_persona_v1 資料保留；未使用真實客戶資料 |
| 18 | 375／390／430 溢出 | 通過，並檢查 768／1440，元素無超出視窗 |
| 19 | Console | 無 JavaScript 執行錯誤；初測原工具遠端字型被環境網路限制擋住，出現 resource error。導覽移除遠端字型；原工具離線功能回歸以空字型 CSS 回應隔離網路，零 console error |
| 20 | GitHub Pages 路徑 | 本機 `/roxyai/` 子路徑驗證通過，相對 CSS 與 CTA 正常；此版尚未 push，線上部署未驗收 |

使用本機 Chrome 無頭瀏覽器測試，並檢視 390 與 1440 截圖。手機寬度模擬不等同 iPhone Safari／LINE 真機測試。保留 reduced-motion；原生 details 支援鍵盤；深陶土主 CTA 白字對比高於 4.5:1。

## 真人驗收

請將導覽交給完全沒有聽過產品的人，不先解釋，詢問：工具做什麼、為什麼需要、下一步是什麼。尚未執行真人訪談，不宣稱通過。

## 交付狀態

本機預覽已啟動；變更在本機 repository 提交。未 push，未改動正式 GitHub Pages。提交 hash 由交付訊息回報。
