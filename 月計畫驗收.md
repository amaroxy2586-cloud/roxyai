# 月計畫訪談流程（2026-09-24）

本次修改 `index.html`，保留目前線上週計畫與其他產生器，不改人設資料結構、儲存 key、匯出／匯入格式，也不新增 AI API。

## 操作

開始製作 → 週／月計劃 → 月計畫 → 選年份月份 → 單選方案 → 選全月限動發布日數 1～5 天 → 複製訪談指令到 AI。

| 方案 | Reels | 輪播 | 每個限動發布日 |
|---|---:|---:|---:|
| A 輕量款 | 4 篇 | 4 篇 | 約 3 篇 |
| B 中階款 | 8 篇 | 4 篇 | 約 3 篇 |
| C 高階經營款 | 12 篇 | 6 篇 | 約 5 篇 |

發布日數指整個月，不是每週。缺少月份、方案、限動天數時顯示提示，輸出保持空白，複製／儲存也會阻擋。舊版月計劃草稿仍能辨識，但不自動替使用者選月份或換算方案。

每組人設各自保存月計畫草稿。指令使用目前選用人設，排除舊模板「先問五個背景問題」「一次列出三個問題」的衝突。訪談最多 8 個主要問題＋3 個必要追問，一次一題；資訊足夠即可停止。最終包含素材整理、八部分規劃與篇數核對。

## 已完成的本機驗收

- 真實 Edge 瀏覽器自動操作 A／B／C × 限動 1～5 天，共 15 種組合，逐項核对動態篇數與單選狀態。
- 缺少每個必填欄位，以及無效月份、方案與天數，皆無法生成完整指令。
- 測試 2028 年 2 月，指令正確標示月底為 29 日；四週安排涵蓋整月。
- 兩組假人設切換時背景同步更新、草稿獨立；重新整理後人設與草稿保留。
- 已生成月指令存入指令庫，匯出 JSON，再於乾淨瀏覽器測試合併及覆蓋匯入，兩組人設與完整指令保留。
- 以修改前線上檔案為基準，比對週計畫、海報、輪播、簡報、短影音的預設與已填資料輸出，逐字相同。
- 320、375、390、768、1440px 畫面未出現整頁橫向溢出，方案卡片皆可點選；檢視手機截圖。
- 瀏覽器無執行錯誤；JavaScript 語法及 git diff 空白檢查通過。

## 自行驗收

1. 選 2026 年 10 月、方案 B、限動 4 天，檢查輸出設定為 Reels 8 篇、輪播 4 篇、限動 4 個發布日、每日約 3 篇。
2. 改選 A 與 C，確認數字分別變成 4／4／3 與 12／6／5，限動發布日數保持 4 天。
3. 清空表單後重選月計畫，缺任何一項時不能複製或存入指令庫。
4. 切換人設確認背景，重新整理後再看草稿。
5. 複製到自己使用的 AI，確認只先說明方案並問第一題；回答後才繼續訪談，最後核對八部分與數量。

## 邊界與既有行為

- 網站生成指令，不在站內執行訪談；外部 AI 是否全程遵守提問上限與篇數，仍須在實際對話驗收。指令已要求自我核對，但網站不能控制 AI 回覆。
- 備份維持原本只包含人設與指令庫；尚未存進指令庫的表單草稿仍只留在本機，不新增草稿備份格式。
- 使用者時間不足時先降低製作負擔或替換素材；若要減少總篇數，指令要求 AI 先取得使用者同意。
- 原資料夾 `C:\Users\User\roxyai` 尚未提交的週計畫修改完整保留；本次使用獨立副本 `C:\Users\User\OneDrive\Desktop\ai\roxyai-month-update`，不將該週計畫修改一併上線。
